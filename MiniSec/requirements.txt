# MiniSec runtime dependencies
#
# Install:   pip install -r requirements.txt
#
# NOTE: this file is intentionally ASCII-only (no Chinese comments).
#   pip reads a requirements file using the *system locale* encoding on Windows
#   (pip's auto_decode only special-cases a UTF-8 BOM). On a Chinese Windows
#   system the locale is GBK, so UTF-8 comments here make `pip install -r`
#   fail with UnicodeDecodeError before it installs anything.
#   Chinese explanations live in README.md and PROJECT_CONTEXT.md instead.
#
# venv/ is not tracked in git, so this file is what rebuilds the environment
# on another machine. Versions match the development environment
# (Python 3.10 / Flask 3.1.3 / Werkzeug 3.1.8).

Flask==3.1.3
Werkzeug==3.1.8
